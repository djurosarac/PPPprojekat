@model IEnumerable<PPPprojekat.StavkaOtpremnice>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kolicina)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Opis)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Proizvod.Naziv)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kolicina)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Opis)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Proizvod.Naziv)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RbStavkeOtpremnice }) |
            @Html.ActionLink("Details", "Details", new { id=item.RbStavkeOtpremnice }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RbStavkeOtpremnice })
        </td>
    </tr>
}

</table>
